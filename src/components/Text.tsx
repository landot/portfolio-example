import styled from "styled-components";
import { Colors } from "../assets/colors";

export const Text = styled.p`
    font-style: normal;
    font-weight: 500;
    font-size: 18px;
    line-height: 28px;
    color: ${Colors.gray};
    margin: 0;
`