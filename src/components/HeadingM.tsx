import styled from "styled-components";
import { Colors } from "../assets/colors";

export const HeadingM = styled.h3`
    font-style: normal;
    font-weight: 700;
    font-size: 24px;
    line-height: 32px;
    color: ${Colors.white};
    margin: 0;
`